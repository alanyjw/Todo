language: objective-c
osx_image: xcode7.3

cache:
  - bundler
  - carthage

before_script:
  - carthage build --platform iOS

script:
  - open -b com.apple.iphonesimulator
  - ./test.sh && XCODEPROJ="Todo.xcodeproj" bundle exec cucumber
